# SidePar 构建日志

> **日期**: 2025-04-25  
> **项目**: SidePar — macOS 剪贴板 & 提示词管理工具  
> **技术栈**: Tauri 2 + Rust + Vanilla JS/CSS + SQLite  
> **目标平台**: macOS Apple Silicon (aarch64)

---

## 1. 项目初始化

使用 `create-tauri-app` 脚手架创建项目，选择 Vanilla (HTML/CSS/JS) 前端模板。

```bash
npx create-tauri-app@latest sidepar
```

### 依赖配置 (`Cargo.toml`)

```toml
[dependencies]
tauri = { version = "2", features = ["tray-icon", "macos-private-api"] }
tauri-plugin-opener = "2"
tauri-plugin-clipboard-manager = "2"
tauri-plugin-global-shortcut = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v4"] }
rusqlite = { version = "0.31", features = ["bundled"] }
```

### 权限配置 (`capabilities/default.json`)

授予 clipboard-manager、global-shortcut、window 控制和事件通信权限。

---

## 2. Rust 后端实现

### 核心文件: `src-tauri/src/lib.rs`

#### 2.1 数据模型

```rust
pub struct ClipboardItem {
    id: String, content: String, preview: String,
    timestamp: u64, pinned: bool,
}

pub struct PromptItem {
    id: String, title: String, content: String,
    category_id: String, created_at: u64, updated_at: u64,
}

pub struct Category {
    id: String, name: String, color: String, order: u32,
}
```

#### 2.2 SQLite 持久化

数据库路径: `~/Library/Application Support/com.sidepar.app/sidepar.db`

三张表:
- `clipboard_history` — 剪贴板历史 (最多 50 条)
- `prompts` — 提示词
- `categories` — 分类 (含 4 个默认分类: AI提示/代码/邮件/写作)

#### 2.3 窗口定位

```rust
fn position_window_right(window: &tauri::WebviewWindow) {
    // 避开菜单栏 (38px) 和 Dock (8px)
    // 右侧边距 6px 形成浮动卡片效果
    let x = offset_x + screen_w - win_width - edge_gap;
    let y = offset_y + menu_bar_height;
    let height = screen_h - menu_bar_height - dock_margin;
}
```

#### 2.4 托盘图标 & 面板切换

```rust
// 关键修复: 只响应 MouseButtonState::Up, 避免双击事件
if let TrayIconEvent::Click {
    button_state: MouseButtonState::Up, ..
} = event {
    // 延迟 100ms 让 macOS 处理完点击事件
    std::thread::spawn(move || {
        std::thread::sleep(Duration::from_millis(100));
        toggle_panel(&window, &app_handle);
    });
}
```

#### 2.5 剪贴板监控线程

独立线程每 500ms 轮询系统剪贴板，检测到新内容后:
1. 去重 (删除相同 content 的旧记录)
2. 插入 SQLite
3. 通过 `window.emit("clipboard-changed", &item)` 推送到前端

#### 2.6 失焦自动隐藏

```rust
// 窗口失焦时自动滑出
// 800ms 保护期: 防止托盘点击导致的瞬间失焦误触发隐藏
window.on_window_event(move |event| {
    if let WindowEvent::Focused(false) = event {
        if now_ms() - *last_shown_at < 800 { return; }
        // 发送隐藏动画 → 300ms 后真正 hide
    }
});
```

#### 2.7 IPC 命令清单

| 命令 | 功能 |
|------|------|
| `get_clipboard_history` | 获取历史 (pinned优先, 按时间倒序) |
| `clear_clipboard_history` | 清空历史 |
| `delete_history_item` | 删除单条 |
| `toggle_pin_item` | 置顶/取消置顶 |
| `copy_to_clipboard` | 更新最后剪贴板记录 (防止重复检测) |
| `get_prompts` | 获取所有提示词 |
| `save_prompt` | 新建/更新提示词 |
| `delete_prompt` | 删除提示词 |
| `get_categories` | 获取分类列表 |
| `save_category` | 新建/更新分类 |
| `delete_category` | 删除分类 |

---

## 3. 前端实现

### 3.1 HTML 结构 (`src/index.html`)

- 面板容器 (右侧固定 400px)
- 搜索栏
- Tab 切换 (历史记录 / 提示词)
- 历史记录列表 + 清空按钮
- 提示词列表 + 分类筛选栏 + 新建按钮
- 提示词编辑弹窗 (标题/分类/内容)
- 分类管理弹窗 (列表/添加/删除)

### 3.2 CSS 设计系统 (`src/styles/index.css`)

- **主题**: 深色毛玻璃 (Dark Glassmorphism)
- **背景**: `rgba(30, 30, 40, 0.85)` + `backdrop-filter: blur(40px)`
- **动画**: `translateX` 滑入/滑出, `cubic-bezier(0.16, 1, 0.3, 1)` 缓动
- **字体**: Inter (Google Fonts)
- **圆角浮动卡片**: `border-radius: 14px` + `box-shadow`
- 自定义滚动条、hover 微动画、复制闪烁反馈

### 3.3 JavaScript 逻辑 (`src/scripts/app.js`)

- **面板显示时**: 从 SQLite 重新加载所有数据 (history + categories + prompts)
- **搜索**: 实时过滤当前 tab 内容
- **历史记录**: 点击复制, 星标置顶, 删除, 清空
- **提示词**: CRUD + 分类筛选, 点击一键复制
- **分类管理**: 添加/删除分类, 自定义颜色
- **Toast 提示**: 操作反馈动画

---

## 4. 开发过程中的 Bug 修复

### 4.1 面板弹出后立即消失

**原因**: `TrayIconEvent::Click` 在 macOS 上触发两次 (MouseDown + MouseUp)，导致 toggle 执行两次。  
**修复**: 只响应 `MouseButtonState::Up`。

### 4.2 托盘点击后窗口失焦

**原因**: macOS 点击托盘图标时焦点留在菜单栏，窗口无法获得焦点。  
**修复**: 延迟 100ms 显示窗口 + 800ms 失焦保护期。

### 4.3 灰色背景闪烁

**原因**: `window-vibrancy` 对整个窗口应用了毛玻璃效果，滑动时整个窗口区域显示灰色。  
**修复**: 移除 `window-vibrancy`，改用 CSS `backdrop-filter` 仅在面板区域模糊。同时设置 `html, body { background: transparent !important }`。

### 4.4 中文截断 Panic

**原因**: `make_preview` 使用字节索引截取字符串，中文字符为 3 字节 UTF-8，截断时落在字符中间。  
**修复**: 改用 `char_indices()` 按字符数截取。

### 4.5 历史记录不更新

**原因**: 面板隐藏时 WebView 收不到 `clipboard-changed` 事件，重新打开时仍显示旧数据。  
**修复**: `panel-show` 事件处理器中从 SQLite 重新加载所有数据。

---

## 5. 构建产物

```
目标: aarch64-apple-darwin (Apple Silicon)
编译时间: ~4 分钟
```

| 产物 | 大小 | 路径 |
|------|------|------|
| SidePar.app | 11 MB | `src-tauri/target/aarch64-apple-darwin/release/bundle/macos/` |
| SidePar_0.1.0_aarch64.dmg | 4.2 MB | `src-tauri/target/aarch64-apple-darwin/release/bundle/dmg/` |

---

## 6. 项目结构

```
sidepar/
├── package.json
├── src/
│   ├── index.html          # 主 UI 结构
│   ├── styles/
│   │   └── index.css       # 深色毛玻璃设计系统
│   └── scripts/
│       └── app.js          # 前端交互逻辑
└── src-tauri/
    ├── Cargo.toml           # Rust 依赖
    ├── tauri.conf.json      # Tauri 配置 (窗口/透明/无边框)
    ├── capabilities/
    │   └── default.json     # 权限声明
    └── src/
        └── lib.rs           # 完整 Rust 后端 (~480 行)
```

---

## 7. 仓库

- **GitHub**: https://github.com/Neonity2020/sidepar
- **分支**: main
- **首次提交**: `e3ec6b4` — feat: SidePar v0.1.0

---

## 8. 使用方式

### 开发模式
```bash
npm install
npm run tauri dev
```

### 生产构建 (Apple Silicon)
```bash
npm run tauri build -- --target aarch64-apple-darwin
```

### 快捷键
- **Cmd+Shift+V** — 切换面板显示/隐藏
- 点击菜单栏图标 — 切换面板
- 点击面板外区域 — 自动隐藏
